const svgoBrandIconsConfig = {
  plugins: [
    {
      name: 'preset-default',
      params: {
        overrides: {
          cleanupIDs: false,
          removeViewBox: false
        },
      },
    },
  ]
};

module.exports = svgoBrandIconsConfig;

