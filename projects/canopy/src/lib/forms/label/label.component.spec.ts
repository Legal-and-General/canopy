import { LgLabelComponent } from './label.component';

describe('LgLabelComponent', () => {
  it('should create an instance', () => {
    const directive = new LgLabelComponent();

    expect(directive).toBeTruthy();
  });
});
