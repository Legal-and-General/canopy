import { Meta, Markdown, Source } from '@storybook/addon-docs/blocks';
import Feedback from '../../docs/common/feedback.md?raw';
import * as BreadcrumbStories from './breadcrumb.stories';

<Meta title="Components/Breadcrumb/Guide" />

# Breadcrumb

<p class="standfirst">Breadcrumbs help users to understand a site's hierarchical structure and allow them to move between levels.</p>

### Design guidance for this component is currently being rewritten. Please contact the Canopy team if you need more information on how to use this component.

## Usage

Use breadcrumbs to help users to understand where they are within the site's structure and allow them to navigate between levels quickly. They are location-based and NOT used to show a user's actual journey.

You won't need breadcrumbs if your site has a shallow or flat structure, and they should never duplicate main menu or secondary navigation options.

Import the breadcrumb components in your application:

```js
import { LgBreadcrumbComponent } from '@legal-and-general/canopy';
```

and in your HTML:

```html
<lg-breadcrumb>
  <lg-breadcrumb-item>
    <a href="#">
      <lg-icon [name]="'home-outline'"></lg-icon>
      Home
    </a>
  </lg-breadcrumb-item>
  <lg-breadcrumb-item>
    <a href="#">Products</a>
  </lg-breadcrumb-item>
  <lg-breadcrumb-item>
    <span aria-current="page">Pension</span>
  </lg-breadcrumb-item>
</lg-breadcrumb>
```

## How it works

The breadcrumbs component should display the user's current page as text, not a clickable link.

There are two layout variants (`page` and `embedded`) that control whether the breadcrumb is used full-width on a page or embedded within another component.

On mobile breakpoints the current page will not be shown, only the next level up in the hierarchy.

### Showing more than 3 breadcrumbs on desktop

For desktop breakpoints – if there are too many levels of the structure to display, the breadcrumb collapses to show the top layer, current page and the page above. The levels in between will be represented by '...' and won't be interactive.

This is achieved by using `LgBreadcrumbItemEllipsisComponent`:

```html
<lg-breadcrumb-item-ellipsis></lg-breadcrumb-item-ellipsis>
```

Note: _This is not automatically added and should be implemented in your app as per example below._

**A sensible website structure and page naming convention should mean this treatment is rarely used.**

#### Example

<Source of={BreadcrumbStories.Ellipsis} />

## Dos and Don'ts

### Do

1. **Do** make the first breadcrumb a link to the homepage.
2. **Do** reflect the user's position in the hierarchical structure.
3. **Do** include the current page in the breadcrumb trail.

### Don't

1. **Don't** reflect the user's session history.
2. **Don't** make the current page's breadcrumb a link.
3. **Don't** use for websites with a shallow or flat structure.
4. **Don't** duplicate main menu or secondary navigation options.

## Additional development details

### LgBreadcrumbComponent inputs

| Name      | Description                                    | Type     | Default | Required |
| --------- | ---------------------------------------------- | -------- | ------- | -------- |
| `variant` | The layout variants available: `page`, `embedded` | `string` | `page`  | No       |

---

## Research on this pattern

This component is based on best practice and has not had specific usability testing.

## Further reading

<a href="https://www.nngroup.com/articles/breadcrumbs/" target="_blank">
  Breadcrumbs: 11 Design Guidelines for Desktop and Mobile
</a>

<Markdown>{Feedback}</Markdown>
