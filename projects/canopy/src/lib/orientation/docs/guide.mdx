import { Meta, Markdown } from "@storybook/addon-docs/blocks";
import Feedback from '../../docs/common/feedback.md?raw';
import { CustomBadge } from 'storybook-addon-tag-badges/manager-helpers'

<Meta title="Helpers/Directives/Orientation/Guide" tags={['pending']}/>

# Orientation

<CustomBadge text="Pending" style={{ backgroundColor: '#005dba', borderColor: '#005dba', color: '#fff' }} /> <span class="small-text">Due for brand modernisation</span>

The orientation directive arranges the direct children of the host element in a vertical or horizontal layout, in a responsive way.
It accepts an object, where the **key** is a mobile-first breakpoint from the standard list of available breakpoints: `sm`, `md`, `lg`, `xl`, `xxl`.
The **value** is from the set of available Orientation options.
The current available options for Orientation are `vertical`, `horizontal`, `reverse-vertical` and `reverse-horizontal`.

## Usage

Import the orientation directive in your application:

```js
@NgModule({
  ...
  imports: [ ..., LgOrientationDirective ],
});
```

In your HTML, apply the directive to the relevant component. If the default `lgOrientation` attribute has no value, the default orientation will remain.

`LgCardComponent` applies component-specific orientation styling and supports `vertical` and `horizontal` layouts only. `reverse-vertical` and `reverse-horizontal` remain available for other layouts that use `LgOrientationDirective` directly.

Pass in a **responsive orientation object** (see below).

```html
<lg-card [lgOrientation]="{ md: 'horizontal', lg: 'vertical' }"></lg-card>
```

## Responsive orientation

### Responsive orientation object

Use a responsive orientation object which will apply different layout orientations at the specified breakpoints, as defined by the `OrientationResponsive` type. Example:

```js
{
  md: "horizontal",
  lg: "vertical"
}
```

Each **key** is a mobile-first breakpoint from the standard list of available breakpoints: `sm`, `md`, `lg`, `xl`, `xxl`.

The **value** is from the set of available Orientation options.
The current available options for Orientation are `vertical`, `horizontal`, `reverse-vertical` and `reverse-horizontal`.

In the example above, the orientation at the `md` breakpoint will be `horizontal`, and at the `lg` will be `vertical`.

### Orientation options

Have this example in mind:

```html
<div [lgOrientation]="{ sm: 'vertical' }">
  <div>First item</div>
  <div>Second item</div>
</div>
```

**vertical** - arranges the direct children of the host element in a vertical layout. The children maintain the same order and the first item remains at the top.

**horizontal** - arranges the direct children of the host element in a horizontal layout. The children maintain the same order and the first item remains on the left.

**reverse-vertical** - arranges the direct children of the host element in a **reverse** vertical layout. The children do not maintain the same order and the last item is displayed at the top.

**reverse-horizontal** - arranges the direct children of the host element in a **reverse** horizontal layout. The children do not maintain the same order and the last item is displayed on the left.

For cards, use `vertical` and `horizontal` only.

### Examples

At the `sm` breakpoint apply `vertical` orientation, then at the `md` breakpoint apply `horizontal` orientation.

```html

<lg-card [lgOrientation]="{ sm: 'vertical', md: 'horizontal' }"></lg-card>

```

At the `sm` breakpoint apply `vertical` orientation, at the `md` breakpoint apply `reverse-horizontal` orientation, then at the `lg` breakpoint apply `vertical` orientation.

```html

<div [lgOrientation]="{ sm: 'vertical', md: 'reverse-horizontal', lg: 'vertical' }">
  <div>First item</div>
  <div>Second item</div>
</div>

```

## Inputs

The current available orientation variants are `vertical`, `horizontal`, `reverse-vertical` and `reverse-horizontal`.

The current available breakpoints are `sm`, `md`, `lg`, `xl`, `xxl`

| Name                  | Description                                                   |          Type           | Default | Required |
|-----------------------|---------------------------------------------------------------|:-----------------------:|:-------:|:--------:|
| `lgOrientation`       | The orientation variant applied: `{ sm: 'vertical' }`         | `OrientationResponsive` | `null`  |    No    |

***

## Related

* [Promotions pattern](./?path=/docs/patterns-promotions-examples--promotions-general-three-card)
* [Margin directive](./?path=/docs/helpers-directives-margin-guide--docs)
* [Row gap directive](./?path=/docs/helpers-directives-row-gap-guide--docs)

<Markdown>
  {Feedback}
</Markdown>
